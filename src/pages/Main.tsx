import {TicTacToeField} from "../wigets";

const Main = () => {
    return (
        <main>
            <TicTacToeField fieldSize={3} cellRelativeSize={200}/>
        </main>
    );
};

export default Main;